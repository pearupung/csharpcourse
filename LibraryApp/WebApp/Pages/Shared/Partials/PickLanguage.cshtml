@model PickLanguageModel

<div class="form-group border">
  
        <p>Choose a publisher</p>
        <label asp-for="LanguagesSelectlist" class="control-label"></label>
        <select asp-for="Language" class="form-control" asp-items="Model.LanguagesSelectlist"></select>
        <button type="submit" name="languagePick" value="pick">Pick!</button>
        <p>or create one yourself:</p>
        <div class="form-group">
            <label asp-for="NewLanguage" class="control-label"></label>
            <input asp-for="NewLanguage" class="form-control"/>
            <span asp-validation-for="NewLanguage" class="text-danger"></span>
        </div>
        <button type="submit" name="languagePick" value="add">Add and pick this one!</button>
</div>