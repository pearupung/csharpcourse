@model Domain.Publisher

<div class="card text-white bg-secondary mb-3" style="max-width: 18rem;">
    <div class="card-header">Book publisher</div>
    <div class="card-body">
        <p class="card-text">@Model.PublisherName</p>
        <button class="btn btn-danger" type="submit" name="deletePublisher" value="@Model.PublisherId">Remove</button>
    </div>
</div>